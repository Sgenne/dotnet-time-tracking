const baseUrl = "http://localhost:8080";

const baseAuthUrl = `${baseUrl}/auth`;

export const LOGIN_URL = `${baseAuthUrl}/login`;
export const REGISTER_URL = `${baseAuthUrl}/register`;

const baseProjectUrl = `${baseAuthUrl}/project`;
